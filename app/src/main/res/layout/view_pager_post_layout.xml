<?xml version="1.0" encoding="utf-8"?><!-- This linear layout contains a single item displayed in the MainActivity -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/view_pager_post_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorLightGray"
    android:baselineAligned="true"

    android:scaleType="fitXY"
    tools:context="com.example.segev.restaurants.MainScreenActivity">

    <TextView
        android:id="@+id/viewpager_post_title"
        android:layout_width="368dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/view"
        android:layout_centerHorizontal="true"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/black"
        android:textSize="24sp"
        android:textStyle="bold" />

    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="261dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="0dp"
        android:background="@android:drawable/ic_menu_report_image"
        app:srcCompat="@android:drawable/ic_menu_report_image" />

    <TextView
        android:id="@+id/viewpager_post_location"
        android:layout_width="216dp"
        android:layout_height="19dp"
        android:layout_alignStart="@+id/viewpager_post_title"
        android:layout_below="@+id/viewpager_post_title" />

</RelativeLayout>